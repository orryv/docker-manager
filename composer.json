{
    "name": "orryv/docker-manager",
    "type": "library",
    "autoload": {
        "psr-4": {
            "Orryv\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^11.4",
        "symfony/yaml": "^7.0"
    },
    "authors": [
        {
            "name": "Orry"
        }
    ],
    "require": {
        "orryv/x-string": "^1.0",
        "orryv/tools": "^1.0",
        "orryv/path": "v3.x-dev"
    },
    "suggest": {
        "ext-yaml": "Required for the default YAML parser implementation.",
        "symfony/yaml": "Provides a pure PHP YAML parser alternative."
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    }
}
